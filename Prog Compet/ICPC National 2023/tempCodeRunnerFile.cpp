#include<iostream>
#define ll long long int
#define si short int

 using namespace std;

int main(){
    int n;
    bool w;

    cin >> n;
    int numbers[3 * n];
    bool numbersIndicator[sizeof(numbers)];

    memset(numbersIndicator, 0, sizeof(numbers));

    for(auto i = 0; i++ < treN;)
        cin >> numbers[i];

    for(auto i = 0; i++ < treN;)
        cout << numbers[i];
    
}